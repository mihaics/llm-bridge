# Copy to deploy/.env (gitignored) and set values. docker-compose.yml reads this automatically.
# LLM_BRIDGE_TOKEN MUST match server.bearer_token in deploy/config.yaml.
LLM_BRIDGE_TOKEN=REPLACE-with-a-strong-token-min-16-chars
SHIM_PORT=8090
OWUI_PORT=3000
